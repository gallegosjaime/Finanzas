<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Finanzas.Views.UserPage"
             Title="UserPage"
             NavigationPage.HasBackButton="False">
    <ContentPage.Content>
        <StackLayout Grid.Row="1" VerticalOptions="End" HorizontalOptions="FillAndExpand"  Margin="0,0,0,10">
            <Label Text="{Binding User, StringFormat='Welcome {0}'}" Margin="10"/>
            <Entry Text="{Binding Password}" IsPassword="True" Margin="5,5" Placeholder="Enter new password"/>
            <Button x:Name="updatebtn" Text="Change Password" Command="{Binding UpdateCommand}" />
            <Button x:Name="deletebtn" Text="Delete Account" Command="{Binding DeleteCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>